# 🏥 Медицинский центр - Full Stack приложение

## 📋 Содержание проекта

Полнофункциональное веб-приложение для управления медицинским центром с современным стеком технологий.

### Структура папок:
```
FirstProjectOnFreelance/
├── backend/                 # FastAPI приложение
│   ├── app/                # Основной пакет
│   ├── requirements.txt     # Зависимости Python
│   ├── .env               # Переменные окружения
│   ├── .gitignore
│   └── README.md          # Документация backend
├── frontend/               # React приложение
│   ├── src/               # Исходный код
│   ├── package.json       # Зависимости Node.js
│   ├── vite.config.js     # Конфигурация Vite
│   ├── .gitignore
│   └── README.md          # Документация frontend
├── README.md              # Главная документация
└── QUICKSTART.md          # Этот файл
```

## 🎯 Особенности

### Backend (FastAPI)
- ✅ RESTful API с полной документацией
- ✅ SQLAlchemy ORM для работы с БД
- ✅ Pydantic валидация данных
- ✅ CORS поддержка
- ✅ Interactive Swagger документация

### Frontend (React)
- ✅ Современный интерфейс на React 18
- ✅ Быстрая сборка с Vite
- ✅ React Router для маршрутизации
- ✅ Axios для API запросов
- ✅ Отзывчивый дизайн

### Функциональность
- 👨‍⚕️ Управление врачами (CRUD)
- 👤 Управление пациентами (CRUD)
- 📅 Планирование приемов
- 💊 Управление услугами
- 📊 Просмотр информации

## 🚀 Быстрый старт (5 минут)

### Шаг 1: Backend Setup

```bash
# Перейти в папку backend
cd backend

# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate

# Активировать (Linux/Mac)
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Запустить сервер
python -m uvicorn app.main:app --reload
```

**Результат:**
- Backend запустится на `http://localhost:8000`
- Документация доступна на `http://localhost:8000/docs`
- API health check: `http://localhost:8000/api/health`

### Шаг 2: Frontend Setup

**В новом терминале:**

```bash
# Перейти в папку frontend
cd frontend

# Установить зависимости
npm install

# Запустить dev сервер
npm run dev
```

**Результат:**
- Frontend запустится на `http://localhost:3000`

## ✅ Проверка установки

### Проверить Backend
```bash
curl http://localhost:8000/api/health
# Должен вернуть: {"status":"healthy"}
```

### Проверить Frontend
Откройте `http://localhost:3000` в браузере - должна загрузиться главная страница

## 📚 Основные страницы приложения

### 🏠 Home (`/`)
- Главная страница с информацией о медицинском центре
- Список основных услуг
- Контактные данные

### 👨‍⚕️ Doctors (`/doctors`)
- Список всех врачей
- Добавление нового врача
- Удаление врача
- Информация: имя, специализация, email, телефон, лицензия

### 👤 Patients (`/patients`)
- Список всех пациентов
- Добавление нового пациента
- Удаление пациента
- Информация: имя, email, телефон, дата рождения, адрес, медицинская история

### 📅 Appointments (`/appointments`)
- Список всех приемов
- Бронирование приема
- Выбор врача и пациента
- Статус приема (scheduled/completed/cancelled)
- Удаление приема

### 💊 Services (`/services`)
- Список всех услуг
- Добавление услуги
- Информация: название, описание, цена, длительность
- Статус доступности

## 🔌 API Endpoints

### Doctors
```
GET    /api/doctors              # Получить всех врачей
GET    /api/doctors/{id}         # Получить врача по ID
POST   /api/doctors              # Создать врача
PUT    /api/doctors/{id}         # Обновить врача
DELETE /api/doctors/{id}         # Удалить врача
```

### Patients
```
GET    /api/patients             # Получить всех пациентов
GET    /api/patients/{id}        # Получить пациента по ID
POST   /api/patients             # Создать пациента
PUT    /api/patients/{id}        # Обновить пациента
DELETE /api/patients/{id}        # Удалить пациента
```

### Appointments
```
GET    /api/appointments                    # Получить все приемы
GET    /api/appointments/{id}               # Получить прием по ID
POST   /api/appointments                    # Создать прием
PUT    /api/appointments/{id}               # Обновить прием
DELETE /api/appointments/{id}               # Удалить прием
GET    /api/appointments/doctor/{doctor_id} # Приемы врача
GET    /api/appointments/patient/{patient_id} # Приемы пациента
```

### Services
```
GET    /api/services             # Получить все услуги
GET    /api/services/{id}        # Получить услугу по ID
POST   /api/services             # Создать услугу
PUT    /api/services/{id}        # Обновить услугу
DELETE /api/services/{id}        # Удалить услугу
```

## 💾 Работа с базой данных

База данных (SQLite) создается автоматически при первом запуске backend.

**Расположение БД:**
```
backend/medical_center.db
```

### Сброс БД (удалить все данные)
```bash
# Удалить файл БД
rm backend/medical_center.db

# База создаст заново при следующем запуске
```

## 🔧 Конфигурация

### Backend (.env файл)
```
DATABASE_URL=sqlite:///./medical_center.db
SECRET_KEY=your-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
CORS_ORIGINS=["http://localhost:3000", "http://localhost:8000"]
```

### Frontend (vite.config.js)
```javascript
server: {
  port: 3000,
  proxy: {
    '/api': {
      target: 'http://localhost:8000',
      changeOrigin: true,
    },
  },
}
```

## 📝 Примеры использования API

### Создать врача
```bash
curl -X POST "http://localhost:8000/api/doctors" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Dr. Ivan Petrov",
    "specialization": "Cardiology",
    "email": "ivan@example.com",
    "phone": "+7 (999) 123-45-67",
    "license_number": "MED2024001",
    "bio": "Опытный кардиолог с 10 летним стажем"
  }'
```

### Создать пациента
```bash
curl -X POST "http://localhost:8000/api/patients" \
  -H "Content-Type: application/json" \
  -d '{
    "first_name": "Alexander",
    "last_name": "Smirnov",
    "email": "alex@example.com",
    "phone": "+7 (999) 987-65-43",
    "date_of_birth": "1990-05-15",
    "address": "123 Main Street, Moscow",
    "medical_history": "Гипертония",
    "allergies": "Пенициллин"
  }'
```

### Создать прием
```bash
curl -X POST "http://localhost:8000/api/appointments" \
  -H "Content-Type: application/json" \
  -d '{
    "patient_id": 1,
    "doctor_id": 1,
    "appointment_date": "2024-01-20T10:00:00",
    "duration_minutes": 30,
    "notes": "Консультация и осмотр"
  }'
```

## 🚦 Запуск в режиме разработки

### Терминал 1 (Backend)
```bash
cd backend
source venv/bin/activate  # или venv\Scripts\activate на Windows
python -m uvicorn app.main:app --reload
```

### Терминал 2 (Frontend)
```bash
cd frontend
npm run dev
```

## 🏗️ Production сборка

### Backend
```bash
cd backend
pip install gunicorn
gunicorn -w 4 -k uvicorn.workers.UvicornWorker app.main:app
```

### Frontend
```bash
cd frontend
npm run build
# Папка dist готова для деплоя
```

## 🐛 Решение проблем

### Порт уже занят
```bash
# Backend на другом порту
uvicorn app.main:app --reload --port 8001

# Frontend на другом порту
npm run dev -- --port 3001
```

### CORS ошибки
Убедитесь что backend запущен и разрешает запросы с localhost:3000

### База данных поведет себя странно
```bash
# Удалить и пересоздать
rm backend/medical_center.db
# Запустить backend заново
```

### npm install не работает
```bash
# Очистить кэш
npm cache clean --force
# Установить заново
npm install
```

## 📖 Дополнительная информация

Детальная документация находится в:
- `backend/README.md` - документация FastAPI приложения
- `frontend/README.md` - документация React приложения
- `README.md` - полная документация проекта

## 🎓 Структура базы данных

### Doctors (Врачи)
```
id - уникальный идентификатор
name - имя врача
specialization - специализация
email - электронная почта
phone - телефон
license_number - номер лицензии
bio - биография
is_active - активен ли врач
```

### Patients (Пациенты)
```
id - уникальный идентификатор
first_name - имя
last_name - фамилия
email - электронная почта
phone - телефон
date_of_birth - дата рождения
address - адрес
medical_history - медицинская история
allergies - аллергии
is_active - активен ли пациент
```

### Appointments (Приемы)
```
id - уникальный идентификатор
patient_id - ID пациента
doctor_id - ID врача
appointment_date - дата и время приема
duration_minutes - длительность в минутах
status - статус (scheduled/completed/cancelled)
notes - заметки
created_at - дата создания
```

### Services (Услуги)
```
id - уникальный идентификатор
name - название услуги
description - описание
price - цена
duration_minutes - длительность в минутах
is_available - доступна ли услуга
```

## 🎯 Следующие шаги после установки

1. ✅ Запустить backend и frontend
2. 👨‍⚕️ Добавить нескольких врачей через интерфейс
3. 👤 Добавить пациентов
4. 📅 Забронировать приемы
5. 💊 Добавить услуги
6. 📊 Посмотреть API документацию на `/docs`

## 💡 Советы

- Используйте Swagger UI (`http://localhost:8000/docs`) для тестирования API
- Все данные хранятся в локальной SQLite БД (medical_center.db)
- При разработке фронтенда используйте React DevTools расширение
- Для дебага используйте `console.log()` и браузерные инструменты

## 📞 Контакты и поддержка

Если возникли вопросы, проверьте:
- Терминал с ошибками
- Логи приложения
- Документацию в README файлах
- Документацию официальных библиотек

---

**Версия:** 1.0.0  
**Дата создания:** 2024  
**Статус:** ✅ Ready for development
