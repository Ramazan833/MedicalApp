# Medical Center Management System

Полнофункциональное веб-приложение для управления медицинским центром, созданное на FastAPI и React.

## 📋 Описание

Это приложение предоставляет:
- **Управление врачами** - добавление, редактирование и удаление информации о врачах
- **Управление пациентами** - ведение истории пациентов с медицинской информацией
- **Управление приемами** - планирование и отслеживание приемов пациентов у врачей
- **Управление услугами** - каталог медицинских услуг с ценами и описаниями

## 🏗️ Архитектура

### Backend (FastAPI)
```
backend/
├── app/
│   ├── main.py           # Главное приложение
│   ├── database.py       # Конфигурация БД
│   ├── models/           # SQLAlchemy модели
│   ├── schemas/          # Pydantic схемы для валидации
│   └── routers/          # API роуты
├── requirements.txt      # Зависимости
├── .env                  # Переменные окружения
└── .gitignore
```

### Frontend (React + Vite)
```
frontend/
├── src/
│   ├── pages/            # Страницы приложения
│   ├── components/       # React компоненты
│   ├── services/         # API клиент
│   ├── styles/           # CSS стили
│   └── App.jsx
├── package.json
├── vite.config.js
└── .gitignore
```

## 🚀 Начало работы

### Требования
- Python 3.8+
- Node.js 16+
- npm или yarn

### Backend Setup

1. **Перейти в папку backend:**
```bash
cd backend
```

2. **Создать виртуальное окружение:**
```bash
python -m venv venv
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate
```

3. **Установить зависимости:**
```bash
pip install -r requirements.txt
```

4. **Запустить сервер:**
```bash
python -m uvicorn app.main:app --reload
```

Backend будет доступен на `http://localhost:8000`
Документация API: `http://localhost:8000/docs`

### Frontend Setup

1. **Перейти в папку frontend:**
```bash
cd frontend
```

2. **Установить зависимости:**
```bash
npm install
```

3. **Запустить dev сервер:**
```bash
npm run dev
```

Frontend будет доступен на `http://localhost:3000`

## 📚 API Endpoints

### Doctors
- `GET /api/doctors` - Получить всех врачей
- `GET /api/doctors/{id}` - Получить врача по ID
- `POST /api/doctors` - Создать врача
- `PUT /api/doctors/{id}` - Обновить врача
- `DELETE /api/doctors/{id}` - Удалить врача

### Patients
- `GET /api/patients` - Получить всех пациентов
- `GET /api/patients/{id}` - Получить пациента по ID
- `POST /api/patients` - Создать пациента
- `PUT /api/patients/{id}` - Обновить пациента
- `DELETE /api/patients/{id}` - Удалить пациента

### Appointments
- `GET /api/appointments` - Получить все приемы
- `GET /api/appointments/{id}` - Получить прием по ID
- `POST /api/appointments` - Создать прием
- `PUT /api/appointments/{id}` - Обновить прием
- `DELETE /api/appointments/{id}` - Удалить прием
- `GET /api/appointments/doctor/{doctor_id}` - Приемы врача
- `GET /api/appointments/patient/{patient_id}` - Приемы пациента

### Services
- `GET /api/services` - Получить все услуги
- `GET /api/services/{id}` - Получить услугу по ID
- `POST /api/services` - Создать услугу
- `PUT /api/services/{id}` - Обновить услугу
- `DELETE /api/services/{id}` - Удалить услугу

## 🛠️ Технологический стек

### Backend
- **FastAPI** - современный веб-фреймворк
- **SQLAlchemy** - ORM для работы с БД
- **Pydantic** - валидация данных
- **Uvicorn** - ASGI сервер

### Frontend
- **React** - UI библиотека
- **Vite** - быстрый сборщик
- **Axios** - HTTP клиент
- **React Router** - маршрутизация

## 📝 Структура данных

### Doctor
```json
{
  "id": 1,
  "name": "Dr. John Doe",
  "specialization": "Cardiology",
  "email": "john@example.com",
  "phone": "+1234567890",
  "license_number": "MED123456",
  "bio": "Опытный кардиолог",
  "is_active": true
}
```

### Patient
```json
{
  "id": 1,
  "first_name": "Jane",
  "last_name": "Doe",
  "email": "jane@example.com",
  "phone": "+1234567890",
  "date_of_birth": "1990-01-01",
  "address": "123 Main St",
  "medical_history": "История болезней",
  "allergies": "Пенициллин",
  "is_active": true
}
```

### Appointment
```json
{
  "id": 1,
  "patient_id": 1,
  "doctor_id": 1,
  "appointment_date": "2024-01-15T10:00:00",
  "duration_minutes": 30,
  "status": "scheduled",
  "notes": "Осмотр",
  "created_at": "2024-01-10T12:00:00"
}
```

### Service
```json
{
  "id": 1,
  "name": "Консультация",
  "description": "Первичная консультация врача",
  "price": 50.00,
  "duration_minutes": 30,
  "is_available": true
}
```

## 🔧 Переменные окружения

### Backend (.env)
```
DATABASE_URL=sqlite:///./medical_center.db
SECRET_KEY=your-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
CORS_ORIGINS=["http://localhost:3000", "http://localhost:8000"]
```

## 🚦 Основные функции

- ✅ CRUD операции для всех сущностей
- ✅ Валидация данных через Pydantic
- ✅ CORS поддержка
- ✅ RESTful API
- ✅ Interactive API документация (Swagger)
- ✅ Отзывчивый интерфейс
- ✅ Современный дизайн

## 🔒 Безопасность

- CORS конфигурация
- Валидация входящих данных
- SQLAlchemy защита от SQL инъекций
- Переменные окружения для чувствительных данных

## 📈 Расширение функционала

Для добавления новых функций:

1. **Backend:**
   - Добавить модель в `app/models/`
   - Добавить схему в `app/schemas/`
   - Создать роутер в `app/routers/`

2. **Frontend:**
   - Создать компонент в `src/components/`
   - Создать страницу в `src/pages/`
   - Добавить API методы в `src/services/api.js`
   - Добавить стили в `src/styles/`

## 👨‍💻 Автор

Medical Center Management System

## 📄 Лицензия

MIT License
