# 🏥 Инструкция по запуску на Windows

## 📋 Требования

- **Python** 3.8 или выше
- **Node.js** 16 или выше
- **Git** (опционально)
- **Text Editor/IDE** (VS Code рекомендуется)

## 🔍 Проверка требований

Откройте PowerShell и выполните:

```powershell
python --version  # Должно показать Python 3.8+
node --version    # Должно показать v16+
npm --version     # Должно показать версию npm
```

Если команд нет, скачайте и установите:
- Python: https://www.python.org/downloads/
- Node.js: https://nodejs.org/

## 🚀 Быстрый старт (10 минут)

### ТЕРМИНАЛ 1: Backend

```powershell
# 1. Откройте PowerShell
# 2. Перейдите в папку проекта
cd C:\Users\Ramazan\Desktop\FirstProjectOnFreelance\backend

# 3. Создайте виртуальное окружение
python -m venv venv

# 4. Активируйте его
venv\Scripts\activate

# 5. Установите зависимости
pip install -r requirements.txt

# 6. Запустите сервер
python -m uvicorn app.main:app --reload
```

**Результат:**
```
INFO:     Started server process
INFO:     Uvicorn running on http://127.0.0.1:8000
```

### ТЕРМИНАЛ 2: Frontend (откройте новое окно PowerShell)

```powershell
# 1. Перейдите в папку frontend
cd C:\Users\Ramazan\Desktop\FirstProjectOnFreelance\frontend

# 2. Установите зависимости
npm install

# 3. Запустите dev сервер
npm run dev
```

**Результат:**
```
VITE v5.0.0 ready in 123 ms
➜ Local: http://localhost:3000
```

### БРАУЗЕР

Откройте `http://localhost:3000` - должна загрузиться главная страница!

---

## ✅ Проверка установки

### Тест Backend API:

Откройте новый PowerShell:

```powershell
# Проверить статус
curl http://localhost:8000/api/health
```

Должен вернуть:
```json
{"status":"healthy"}
```

### Тест Frontend:

Откройте `http://localhost:3000` в браузере.

---

## 🛑 Остановка приложения

- **Backend**: Нажмите `Ctrl + C` в терминале backend
- **Frontend**: Нажмите `Ctrl + C` в терминале frontend

---

## 📚 API Документация

Откройте в браузере:
```
http://localhost:8000/docs
```

Здесь вы сможете тестировать все API endpoints.

---

## 🎯 Основные команды Windows PowerShell

```powershell
# Перейти в папку
cd путь/к/папке

# Вернуться на уровень выше
cd ..

# Вернуться на главный диск
cd C:\

# Посмотреть файлы в папке
ls
# или
dir

# Создать новую папку
mkdir имя_папки

# Удалить файл
rm имя_файла

# Очистить экран
cls

# Скопировать файл
copy источник назначение
```

---

## 🐛 Решение проблем на Windows

### Проблема: "venv не найден"

**Решение:**
```powershell
# Проверьте, что находитесь в правильной папке
cd backend

# Попробуйте создать заново
python -m venv venv
```

### Проблема: "pip install не работает"

**Решение:**
```powershell
# Убедитесь, что виртуальное окружение активировано
venv\Scripts\activate

# Проверьте с полным путем
python -m pip install -r requirements.txt
```

### Проблема: "Порт 8000/3000 уже используется"

**Решение 1 - Использовать другой порт:**
```powershell
# Backend на порте 8001
python -m uvicorn app.main:app --reload --port 8001

# Frontend на порте 3001
npm run dev -- --port 3001
```

**Решение 2 - Найти и остановить процесс:**
```powershell
# Найти процесс на порте 8000
netstat -ano | findstr :8000

# Остановить процесс (замените PID на число из результата выше)
taskkill /PID число /F
```

### Проблема: "npm: команда не найна"

**Решение:**
```powershell
# Переустановите Node.js с https://nodejs.org/

# После установки перезагрузитесь

# Проверьте версию
npm --version
```

### Проблема: CORS ошибки

**Решение:**
```powershell
# Убедитесь, что backend запущен на http://localhost:8000
# Убедитесь, что frontend обращается к http://localhost:8000

# Если все равно ошибка, проверьте backend/app/main.py
```

### Проблема: База данных работает странно

**Решение:**
```powershell
# Удалить файл БД
cd backend
rm medical_center.db

# Запустить backend заново (создаст новую БД)
python -m uvicorn app.main:app --reload
```

---

## 📁 Проверка файлов проекта

Убедитесь, что у вас есть эти файлы:

```
✓ backend/
  ✓ app/main.py
  ✓ app/database.py
  ✓ app/models/doctor.py
  ✓ app/schemas/doctor.py
  ✓ app/routers/doctors.py
  ✓ requirements.txt
  ✓ .env

✓ frontend/
  ✓ src/App.jsx
  ✓ src/main.jsx
  ✓ src/pages/Home.jsx
  ✓ package.json
  ✓ vite.config.js

✓ README.md
✓ QUICKSTART.md
✓ SAMPLE_DATA.md
✓ PROJECT_STRUCTURE.md
```

---

## 🔄 Полный цикл разработки

### День 1: Первый запуск

1. Откройте PowerShell
2. `cd backend`
3. Создайте и активируйте виртуальное окружение
4. Установите зависимости
5. Запустите backend

В новом терминале:
1. `cd frontend`
2. Установите npm зависимости
3. Запустите frontend
4. Откройте http://localhost:3000

### День 2+: Дальнейшая разработка

Каждый день просто запустите в двух терминалах:

**Терминал 1 (Backend):**
```powershell
cd backend
venv\Scripts\activate
python -m uvicorn app.main:app --reload
```

**Терминал 2 (Frontend):**
```powershell
cd frontend
npm run dev
```

---

## 📊 Структура данных

### Doctor (Врач)
- name: Имя
- specialization: Специализация
- email: Email
- phone: Телефон
- license_number: Номер лицензии

### Patient (Пациент)
- first_name: Имя
- last_name: Фамилия
- email: Email
- phone: Телефон
- date_of_birth: Дата рождения
- address: Адрес
- medical_history: Медицинская история
- allergies: Аллергии

### Service (Услуга)
- name: Название
- description: Описание
- price: Цена
- duration_minutes: Длительность

### Appointment (Прием)
- patient_id: ID пациента
- doctor_id: ID врача
- appointment_date: Дата/время
- status: Статус

---

## 💻 Рекомендуемая настройка IDE

### Visual Studio Code (рекомендуется)

**Расширения для установки:**
- Python (Microsoft)
- Pylance (для Python)
- ES7+ React/Redux/React-Native (для JavaScript)
- REST Client (для тестирования API)
- Thunder Client (альтернатива REST Client)

**Конфигурация VS Code:**
```json
{
  "python.defaultInterpreterPath": "${workspaceFolder}/backend/venv/bin/python",
  "python.formatting.provider": "black",
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode"
}
```

---

## 🔗 Полезные ссылки

- **Python**: https://python.org
- **Node.js**: https://nodejs.org
- **FastAPI Docs**: https://fastapi.tiangolo.com
- **React Docs**: https://react.dev
- **Vite Docs**: https://vitejs.dev
- **VS Code**: https://code.visualstudio.com

---

## 🎓 Первые шаги после запуска

1. ✅ Откройте http://localhost:3000 - главная страница
2. ✅ Перейдите на страницу Doctors
3. ✅ Нажмите "Add Doctor"
4. ✅ Заполните форму и добавьте врача
5. ✅ Посмотрите как данные отображаются
6. ✅ Откройте http://localhost:8000/docs
7. ✅ Протестируйте API endpoints через Swagger
8. ✅ Добавьте еще данных через фронтенд или Swagger

---

## 💡 Советы для разработки

- **Hot reload**: Оба сервера (frontend и backend) поддерживают горячую перезагрузку - просто сохраните файл
- **DevTools**: Откройте F12 для просмотра сети и консоли
- **API Docs**: Используйте http://localhost:8000/docs для тестирования API
- **Terminal**: Разместите два терминала рядом для удобства

---

## 🚀 Развертывание на сервер

После разработки:

1. **Backend**: Используйте Heroku, PythonAnywhere или собственный сервер
2. **Frontend**: Используйте Vercel, Netlify или Heroku
3. **База данных**: Используйте PostgreSQL/MySQL вместо SQLite

Детали в README.md файлах.

---

## ✨ Готово!

Проект полностью готов к разработке. Начните с простого добавления врачей и пациентов!

**Если возникли вопросы:**
- Проверьте файлы README.md
- Посмотрите QUICKSTART.md
- Ищите ошибки в консоли (терминала и браузера)

Удачи в разработке! 🚀
