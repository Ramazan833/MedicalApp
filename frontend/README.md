# Medical Center Frontend (React + Vite)

Фронтенд приложение для управления медицинским центром, созданное на React и Vite.

## 🚀 Быстрый старт

### 1. Установить зависимости
```bash
npm install
```

### 2. Запустить dev сервер
```bash
npm run dev
```

Приложение откроется на `http://localhost:3000`

### 3. Собрать production версию
```bash
npm run build
```

### 4. Preview production версии
```bash
npm run preview
```

## 📁 Структура проекта

```
frontend/
├── src/
│   ├── pages/              # Страницы приложения
│   │   ├── Home.jsx        # Главная страница
│   │   ├── Doctors.jsx     # Управление врачами
│   │   ├── Patients.jsx    # Управление пациентами
│   │   ├── Appointments.jsx# Управление приемами
│   │   └── Services.jsx    # Управление услугами
│   ├── components/         # React компоненты
│   │   └── Navigation.jsx  # Навигация
│   ├── services/           # API клиент
│   │   └── api.js          # Axios инстанс
│   ├── styles/             # CSS стили
│   │   ├── App.css
│   │   ├── Navigation.css
│   │   ├── Home.css
│   │   ├── Doctors.css
│   │   ├── Patients.css
│   │   ├── Appointments.css
│   │   └── Services.css
│   ├── App.jsx             # Главный компонент
│   └── main.jsx            # Точка входа
├── index.html              # HTML шаблон
├── package.json            # Зависимости
├── vite.config.js          # Конфигурация Vite
└── .gitignore
```

## 🔌 Основные страницы

### Home (Главная)
- Приветствие
- Информация о медицинском центре
- Контактные данные
- Основные функции

### Doctors (Врачи)
- Список всех врачей
- Форма добавления врача
- Удаление врача
- Информация: имя, специализация, email, телефон

### Patients (Пациенты)
- Список всех пациентов
- Форма добавления пациента
- Удаление пациента
- Информация: имя, email, телефон, дата рождения, адрес

### Appointments (Приемы)
- Список всех приемов
- Бронирование приема
- Информация врача и пациента
- Статус приема
- Удаление приема

### Services (Услуги)
- Список всех услуг
- Добавление услуги
- Удаление услуги
- Информация: название, описание, цена, длительность

## 🎨 Компоненты

### Navigation
Навигационная панель с ссылками на все страницы

## 🔗 API интеграция

Frontend подключается к Backend через Axios:

```javascript
// API базовый URL
const API_BASE_URL = 'http://localhost:8000/api'

// Примеры использования
import { doctorsAPI, patientsAPI, appointmentsAPI, servicesAPI } from '@/services/api'

// Получить врачей
doctorsAPI.getAll()

// Создать врача
doctorsAPI.create(doctorData)

// Обновить врача
doctorsAPI.update(doctorId, doctorData)

// Удалить врача
doctorsAPI.delete(doctorId)
```

## 🛠️ Технологии

- **React 18** - UI библиотека
- **Vite** - быстрый сборщик
- **React Router DOM** - маршрутизация
- **Axios** - HTTP клиент
- **CSS3** - стили

## 📱 Функциональность

### Doctors (Врачи)
- ✅ Просмотр списка врачей
- ✅ Добавление нового врача
- ✅ Удаление врача
- ✅ Отображение специализации и контактных данных

### Patients (Пациенты)
- ✅ Просмотр списка пациентов
- ✅ Добавление нового пациента
- ✅ Удаление пациента
- ✅ Хранение медицинской истории и аллергий

### Appointments (Приемы)
- ✅ Просмотр всех приемов
- ✅ Бронирование приема
- ✅ Выбор врача и пациента
- ✅ Установка даты и времени
- ✅ Отслеживание статуса
- ✅ Удаление приема

### Services (Услуги)
- ✅ Просмотр доступных услуг
- ✅ Добавление услуги
- ✅ Информация о цене и длительности
- ✅ Удаление услуги

## 🎨 Стилизация

- Современный, чистый дизайн
- Отзывчивая верстка (responsive)
- Цветовая схема: синий (#0066cc), зеленый, оранжевый
- Анимации и переходы
- Карточки для отображения информации

## 🚀 Развертывание

### Vercel
```bash
npm install -g vercel
vercel
```

### Netlify
```bash
npm run build
# Загрузить папку dist на Netlify
```

### Docker
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "run", "preview"]
```

## 📝 Примеры использования

### Получить врачей
```jsx
import { doctorsAPI } from '@/services/api'
import { useEffect, useState } from 'react'

function DoctorsList() {
  const [doctors, setDoctors] = useState([])

  useEffect(() => {
    doctorsAPI.getAll()
      .then(res => setDoctors(res.data))
      .catch(err => console.error(err))
  }, [])

  return (
    <div>
      {doctors.map(doctor => (
        <div key={doctor.id}>{doctor.name}</div>
      ))}
    </div>
  )
}
```

### Добавить врача
```jsx
async function handleAddDoctor(doctorData) {
  try {
    const response = await doctorsAPI.create(doctorData)
    console.log('Doctor created:', response.data)
  } catch (error) {
    console.error('Error:', error)
  }
}
```

## 🔧 Переменные окружения

Создайте `.env.local` файл (если нужно):
```
VITE_API_URL=http://localhost:8000
```

Используйте:
```javascript
const API_URL = import.meta.env.VITE_API_URL
```

## 🐛 Решение проблем

### CORS ошибки
Убедитесь, что Backend запущен и CORS включен в `backend/app/main.py`

### API не доступен
Проверьте что Backend запущен на `http://localhost:8000`

### Порт 3000 занят
```bash
npm run dev -- --port 3001
```

## 📚 Дополнительные ресурсы

- [React документация](https://react.dev)
- [Vite документация](https://vitejs.dev)
- [React Router](https://reactrouter.com)
- [Axios](https://axios-http.com)
